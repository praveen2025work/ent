package com.entitlement.repository;

import com.entitlement.entity.SecurityApplication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SecurityApplicationRepository extends JpaRepository<SecurityApplication, Long> {
} 